apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/backend-api  # Тот самый "мостик" к базе
  - httproute.yaml             # Твой роут, который лежит в этой же папке
# - namespace.yaml             # Если он лежит здесь