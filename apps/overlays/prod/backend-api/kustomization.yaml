apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/backend-api # Ссылка на базу бэкенда
  - ../../../base/redis       # Ссылка на базу редиса
  - httproute.yaml            # Твой роут
  - scaledobject.yaml         # Твоя KEDA