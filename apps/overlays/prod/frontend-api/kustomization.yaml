apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../base/frontend-api  # Тот самый "мостик" к базе
  - httproute.yaml             # Твой роут, который лежит в этой же папке
