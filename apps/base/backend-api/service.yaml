apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: store-backend
  labels:
    monitor: apps
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 80
      targetPort: http-backend
      protocol: TCP