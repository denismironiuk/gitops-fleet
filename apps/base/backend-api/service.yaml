apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    monitor: apps
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - name: http
      port: 8081
      targetPort: http-backend
      protocol: TCP